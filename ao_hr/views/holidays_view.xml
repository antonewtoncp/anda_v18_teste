<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit Form View to Modify it -->
    <record id="ao_hr_leave_form_view" model="ir.ui.view">
        <field name="name">hr.leave.form</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet//group" position="after">
                <group string="Data do Processamento de Subsídio de Férias"
                       attrs="{'invisible': [('leave_type_code', '!=', 'SF')]}">
                    <field name="disc_ali_trans"/>
                    <field name="sf_start" attrs="{'required':[('leave_type_code', '!=', 'SF')]}" class="oe_inline"/>
                    <field name="sf_end" attrs="{'required':[('leave_type_code', '!=', 'SF')]}" class="oe_inline"/>
                    <field name="leave_type_code" invisible="1"/>
                </group>
            </xpath>
        </field>
    </record>

    <record id="edit_holiday_status_form" model="ir.ui.view">
        <field name="name">hr.leave.type</field>
        <field name="model">hr.leave.type</field>
        <field name="inherit_id" ref="hr_holidays.edit_holiday_status_form"/>
        <field name="arch" type="xml">
            <field name="leave_validation_type" position="before">
                <field name="code"/>
            </field>
        </field>
    </record>

    <record id="view_holiday_status_normal_tree" model="ir.ui.view">
        <field name="name">hr.leave.type</field>
        <field name="model">hr.leave.type</field>
        <field name="inherit_id" ref="hr_holidays.view_holiday_status_normal_tree"/>
        <field name="arch" type="xml">
            <field name="display_name" position="after">
                <field name="code"/>
            </field>
        </field>
    </record>

    <record id="view_holidays_status_filter" model="ir.ui.view">
        <field name="name">hr.leave.type</field>
        <field name="model">hr.leave.type</field>
        <field name="inherit_id" ref="hr_holidays.view_holidays_status_filter"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="code"/>
            </field>
        </field>
    </record>

</odoo>